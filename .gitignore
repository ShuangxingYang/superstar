# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/

# 环境变量与密钥(绝不提交)。业务密钥在 data/config.json,已被下方 data/ 忽略。
# 模板 config.example.json / .env.example 是安全的,照常入库。
.env
.env.local
*.key

# 前端
node_modules/
dist/
.vite/

# 运行时数据(会话、SQLite、上传文档、记忆文件)
data/
*.sqlite
*.db
*.jsonl

# Qdrant 向量库本地存储(Docker 挂载卷,二进制数据,绝不入库)
qdrant_storage/

# IDE / OS
.DS_Store
.idea/
.vscode/

# superpowers 脚手架临时目录(进度台账等,非项目产物)
.superpowers/
