# 启动配置(非敏感)——复制为 backend/.env 按需覆盖,不填则用括号内默认值。
# 敏感配置(LLM/embedding 的 api_key 等)不在这里,存在 data/config.json,见 config.example.json。

# HOST=127.0.0.1
# PORT=8000
# DATA_DIR=./data
# QDRANT_URL=http://localhost:6333
